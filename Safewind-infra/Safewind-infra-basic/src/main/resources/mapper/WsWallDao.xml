<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safewind.infra.basic.dao.WsWallDao">

    <resultMap type="com.safewind.infra.basic.entity.WsWall" id="WsWallMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="avatar" column="avatar" jdbcType="VARCHAR"/>
        <result property="delFlag" column="del_flag" jdbcType="VARCHAR"/>
        <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="WsWallMap">
        select id,
               name,
               content,
               avatar,
               del_flag,
               create_by,
               create_time,
               update_by,
               update_time
        from ws_wall
        where del_flag = '0'
          and id = #{id}
    </select>

    <!-- 限制查询数量的弹幕 -->
    <select id="queryByLimit" resultMap="WsWallMap">
        select id,
               name,
               content,
               avatar,
               del_flag,
               create_by,
               create_time,
               update_by,
               update_time
        from ws_wall
        where del_flag = '0'
        order by create_time desc
        limit #{limit}
    </select>

    <!-- 分页查询弹幕列表 -->
    <select id="queryPage" resultMap="WsWallMap">
        select id, name, content, avatar, del_flag, create_by, create_time, update_by, update_time
        from ws_wall
        <where>
            del_flag = '0'
            <if test="queryWall.name != null and queryWall.name != ''">
                and name like concat('%', #{queryWall.name}, '%')
            </if>
            <if test="queryWall.content != null and queryWall.content != ''">
                and content like concat('%', #{queryWall.content}, '%')
            </if>
        </where>
        order by create_time desc
        limit #{pageNum}, #{pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from ws_wall
        <where>
            del_flag = '0'
            <if test="queryWall.name != null and queryWall.name != ''">
                and name like concat('%', #{queryWall.name}, '%')
            </if>
            <if test="queryWall.content != null and queryWall.content != ''">
                and content like concat('%', #{queryWall.content}, '%')
            </if>
        </where>
    </select>

    <select id="countPage" resultType="java.lang.Long">
        select count(1)
        from ws_wall
        <where>
            del_flag = '0'
            <if test="queryWall.name != null and queryWall.name != ''">
                and name like concat('%', #{queryWall.name}, '%')
            </if>
            <if test="queryWall.content != null and queryWall.content != ''">
                and content like concat('%', #{queryWall.content}, '%')
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into ws_wall(name, content, avatar, del_flag, create_by, create_time, update_by, update_time)
        values (#{name}, #{content}, #{avatar}, #{delFlag}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into ws_wall(name, content, avatar, del_flag, create_by, create_time, update_by, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.content}, #{entity.avatar}, #{entity.delFlag}, #{entity.createBy},
            #{entity.createTime},
            #{entity.updateBy}, #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into ws_wall(name, content, avatar, del_flag, create_by, create_time, update_by, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.content}, #{entity.avatar}, #{entity.delFlag}, #{entity.createBy},
            #{entity.createTime},
            #{entity.updateBy}, #{entity.updateTime})
        </foreach>
        on duplicate key update
        name = values(name),
        content = values(content),
        avatar = values(avatar),
        del_flag = values(del_flag),
        create_by = values(create_by),
        create_time = values(create_time),
        update_by = values(update_by),
        update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update ws_wall
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="avatar != null and avatar != ''">
                avatar = #{avatar},
            </if>
            <if test="delFlag != null and delFlag != ''">
                del_flag = #{delFlag},
            </if>
            <if test="createBy != null and createBy != ''">
                create_by = #{createBy},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateBy != null and updateBy != ''">
                update_by = #{updateBy},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <update id="deleteById">
        update ws_wall
        set del_flag = '2'
        where id = #{id}
    </update>

</mapper>
