<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.safewind.infra.basic.dao.WsNewsDao">

    <resultMap type="com.safewind.infra.basic.entity.WsNews" id="WsNewsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="publishTime" column="publish_time" jdbcType="TIMESTAMP"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="externalLink" column="external_link" jdbcType="VARCHAR"/>
        <result property="isLink" column="is_link" jdbcType="VARCHAR"/>
        <result property="delFlag" column="del_flag" jdbcType="VARCHAR"/>
        <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="htmlContent" column="html_content" jdbcType="VARCHAR"/>
        <result property="coverImg" column="cover_img" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="WsNewsMap">
        select id,
               title,
               publish_time,
               content,
               html_content,
               cover_img,
               is_link,
               external_link,
               del_flag,
               create_by,
               create_time,
               update_by,
               update_time
        from ws_news
        where del_flag = '0'
          and id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="WsNewsMap">
        select
        id, title, publish_time, content,html_content,
        cover_img, is_link, external_link, del_flag, create_by, create_time, update_by, update_time
        from ws_news
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="title != null">
                and title = #{title}
            </if>
            <if test="publishTime != null">
                and publish_time = #{publishTime}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="delFlag != null and delFlag != ''">
                and del_flag = #{delFlag}
            </if>
            <if test="createBy != null and createBy != ''">
                and create_by = #{createBy}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateBy != null and updateBy != ''">
                and update_by = #{updateBy}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from ws_news
        <where>
            del_flag='0'
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="title != null">
                and title = #{title}
            </if>
            <if test="publishTime != null">
                and publish_time = #{publishTime}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="delFlag != null and delFlag != ''">
                and del_flag = #{delFlag}
            </if>
            <if test="createBy != null and createBy != ''">
                and create_by = #{createBy}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateBy != null and updateBy != ''">
                and update_by = #{updateBy}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!-- 分页查询 -->
    <select id="queryPage" resultMap="WsNewsMap">
        SELECT
        id, title, publish_time, content, html_content, cover_img, is_link, external_link, del_flag, create_by,
        create_time, update_by, update_time
        FROM ws_news
        <where>
            del_flag = '0'
            <!-- 模糊查询标题 -->
            <if test="query.title != null and query.title !=''">
                AND title LIKE CONCAT('%', #{query.title}, '%')
            </if>
            <!--模糊时间查询-->
            <if test="query.startTime != null and query.startTime != ''">
                <if test="query.endTime != null and query.endTime != ''">
                    AND publish_time BETWEEN #{query.startTime} AND #{query.endTime}
                </if>
            </if>
        </where>
        <!-- 按发布时间倒序排列 -->
        ORDER BY publish_time DESC
        <!-- 分页参数 -->
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countNews" resultType="java.lang.Long">
        SELECT
        count(1)
        FROM ws_news
        <where>
            del_flag = '0'
            <!-- 模糊查询标题 -->
            <if test="query.title != null and query.title !=''">
                AND title LIKE CONCAT('%', #{query.title}, '%')
            </if>
            <!--模糊时间查询-->
            <if test="query.startTime != null and query.startTime != ''">
                <if test="query.endTime != null and query.endTime != ''">
                    AND publish_time BETWEEN #{query.startTime} AND #{query.endTime}
                </if>
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into ws_news(title, publish_time, content, html_content,
                            cover_img, is_link, external_link, del_flag, create_by, create_time, update_by, update_time)
        values (#{title},
                #{publishTime},
                #{content},
                #{htmlContent},
                #{coverImg},
                #{isLink},
                #{externalLink},
                #{delFlag},
                #{createBy},
                #{createTime},
                #{updateBy},
                #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into ws_news(title, publish_time, content,html_content,
        cover_img, is_link,external_link,del_flag, create_by, create_time, update_by, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title},
            #{entity.publishTime},
            #{entity.content},
            #{entity.htmlContent},
            #{entity.coverImg},
            #{entity.isLink},
            #{entity.externalLink},
            #{entity.delFlag},
            #{entity.createBy},
            #{entity.createTime},
            #{entity.updateBy},
            #{entity.updateTime})
        </foreach>

    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into ws_news(title, publish_time, content, html_content,
        cover_img, is_link,external_link, del_flag, create_by, create_time, update_by, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title},
            #{entity.publishTime},
            #{entity.content},
            #{entity.htmlContent},
            #{entity.coverImg},
            #{entity.isLink},
            #{entity.externalLink},
            #{entity.delFlag},
            #{entity.createBy},
            #{entity.createTime},
            #{entity.updateBy},
            #{entity.updateTime})
        </foreach>
        on duplicate key update
        title = values(title),
        publish_time = values(publish_time),
        content = values(content),
        html_content = values(html_content),
        cover_img = values(cover_img),
        is_link = values(is_link),
        external_link = values(external_link),
        del_flag = values(del_flag),
        create_by = values(create_by),
        create_time = values(create_time),
        update_by = values(update_by),
        update_time = values(update_time)

    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update ws_news
        <set>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="publishTime != null">
                publish_time = #{publishTime},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="htmlContent != null and htmlContent != ''">
                html_content = #{htmlContent},
            </if>
            <if test="coverImg != null and coverImg != ''">
                cover_img = #{coverImg},
            </if>
            <if test="isLink != null and isLink != ''">
                is_link = #{isLink},
            </if>
            <if test="externalLink != null and externalLink != ''">
                external_link = #{externalLink},
            </if>
            <if test="delFlag != null and delFlag != ''">
                del_flag = #{delFlag},
            </if>
            <if test="createBy != null and createBy != ''">
                create_by = #{createBy},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateBy != null and updateBy != ''">
                update_by = #{updateBy},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from ws_news
        where id = #{id}
    </delete>

</mapper>
